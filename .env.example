# Copy to .env for local development.
#
# This is the public URL of the TMDB proxy Worker (see ./worker), NOT an API key.
# The TMDB key lives as a Worker secret and never reaches the browser.
# Leave it unset and the app runs on local demo data.
VITE_API_BASE=
